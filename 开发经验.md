# dsh_memory_pg 开发经验

> 每个阶段的经验沉淀。时间精确到时分秒；隐私路径按「可复现但去隐私」处理（保留密码 / 127.0.0.1 / 端口等可运行信息，模糊掉个人绝对路径的敏感部分）。

---

## M0 — 技术验证

> 时间：2026-09-14 14:30–15:20（本次阶段内）

### 踩坑记录

| # | 问题（前期没规划到的 / 版本适配 / 未知） | 解决方案 |
|---|---|---|
| 1 | **权限：写入 DSH_HOME 之外的技能目录被拒**（EPERM，workspace-write 沙箱只允许项目目录写） | 用 `sandbox_permissions: danger-full-access` 重试一次；技能装入 `<DSH_HOME>/skills/<name>/`（本例 `D:\dsharness\data\skills\dsh-plugin-dev`） |
| 2 | **权限：docker exec 操作 PG 容器被沙箱拒**（npipe docker API 访问） | 升级 danger-full-access；或改用 TCP（`localhost:54320` 直连） |
| 3 | **权限：npm cache 写被拒**（`EPERM open ...\_cacache\tmp\...`） | npm view 等读 registry 的命令需要缓存写 → 升级权限；且 npm 10 在部分 peer 图上抛 `Cannot read properties of null (reading 'edgesOut')` → **改用 pnpm**（本项目最终用 pnpm 12） |
| 4 | **版本适配：harness v0.1.3 → v0.1.5-rc.1 契约变化** | 文档里 §9 契约表逐项在 0.1.5-rc.1 源码复核成立（commands 小写无斜杠、compaction pressure/context-overflow、settings 命名空间编译期校验）；**settings 无运行时 `settingsNamespace` helper，命名空间是模板字面量类型** |
| 5 | **命令名 kebab-case 未承诺**：契约只说 "lowercase command name without leading slash"，连字符**不确定** | 用动态 Cordis 插件实测 `commands.register({name:'memory-pg-save'})` 注册成功 → 确认 kebab-case 可用 |
| 6 | **动态插件 Host 环境受限**：无 `fetch`/`process`，只有 ctx/harness/console/btoa/atob/TextEncoder/TextDecoder | 探测结果用「注册模型可见工具」（`harness.defineTool` + `registerTool`）暴露，执行结果对我可见 |
| 7 | **`harness.defineTool` 严格契约**：参数根是开放对象（`additionalProperties:false` 被拒）、必须 `output:{schema,render,presentationMeta?}`、`execute` 必须在 defineTool 参数内（不能 spread 追加） | 三次 define/run 迭代修正到通过；这是动态插件与真实 npm 插件（dsh-plugin-dev）的**不同契约** |
| 8 | **隔离实例启动需要完整 Node exec**：tsx/esbuild spawn 子进程被沙箱拒（`spawn EPERM`）；后台 job 无法升级沙箱 | 前台 `Start-Process` + danger-full-access；`--from-default-profile web` 物化独立 profile（注意 `web` 是 shipped profile 名，不能作 custom target，须用别名如 `m0test`） |
| 9 | **PG 维度强制**：1 维向量插 `vector(1024)` 列报 `expected 1024 dimensions` | 这是**想要的**维度校验行为（§2.2⑥）；用 `array_fill(0.001, ARRAY[1024])::vector` 插入正确数据 |
| 10 | **未知：`tsc`/esbuild 在项目目录外启动失败**（`Cannot find package 'tsx' imported from <cwd>`） | 所有 harness 启动命令的**工作目录必须是 harness 仓库根**（`D:\dsharness\sof\deepseek-harness`） |

### 阶段实现框架

- **形态确认**：bundle 插件（`dsh.bundle.patch` + `dsh.client`），`dsh plugin --profile <p> add` 安装重启生效（非 HMR）。
- **D4 决策**：`ctx.llm.stream()`（路线 A）——inspect 确认 `llm` 服务挂载 + `llm-deepseek` 适配器注册 `PROVIDER`。
- **隔离验证环境**（不碰生产）：
  - 生产 DSH_HOME = `<dshhome>`（隐私化：原为 `D:\dsharness\data`，下文以 `<dshhome>` 代）
  - 测试 DSH_HOME = `<workspace>\.testhome`
  - PG 容器：`127.0.0.1:54320`（库 `postgres`，用户 `postgres`，密码 `czq`；自带 vector 0.8.6 + age 1.8.0 + pg_trgm）
  - Ollama：`127.0.0.1:11434`（embedding `bge-m3`，1024 维）
- **HNSW 验证**：建 `dsh_memory_pg_test` 库 → `CREATE EXTENSION vector/pg_trgm` → 建 facts 表 + 1024 维数据 + `hnsw (embedding vector_cosine_ops)` 索引 → 查询可用。

### 调试与隔离方案（M0 段）

1. **隔离 DSH_HOME**：`$env:DSH_HOME = "<workspace>\.testhome"`（绝不指向生产 `<dshhome>`）。
2. **物化独立 profile**：
   ```powershell
   cd <harness-root>
   node --import tsx/esm apps/cli/src/bin.ts --profile m0test --from-default-profile web --dump-config
   ```
   （`web` 是 shipped 名，别名 `m0test` 才能自定义。）
3. **启动隔离实例**（保持运行供调试）：
   ```powershell
   cd <harness-root>
   $env:DSH_HOME = "<workspace>\.testhome"
   Start-Process node -ArgumentList "--import","tsx/esm","apps/cli/src/bin.ts","--profile","m0test","--no-open","--port","3099" -WorkingDirectory "<harness-root>" -RedirectStandardOutput "<workspace>\.testhome\web-m0test.log" -RedirectStandardError "<workspace>\.testhome\web-m0test.err.log"
   ```
   ready 行（日志）→ `dsh web: http://127.0.0.1:3099/?token=<43字符>` → 浏览器打开即登录。

### token 使用情况

无法统计上传/下载 token 分项（当前会话无此计量工具），不填。

### 使用的插件和 skill

- Skill：`cordis-plugin-development`（动态插件开发工作流）、`dsh-plugin-dev`（bundle 形态与 npm 路径）、`brainstorming`（早期需求评审）、`make-dsh-plugin`（早期形态选型）
- 动态插件：`mopr-1`（M0 探测：kebab 命令注册 + llm 运行时探测，验证后 stop，定义保留）

---

## M1 — 骨架与设置面板

> 时间：2026-09-14 15:20–15:47

### 踩坑记录

| # | 问题（前期没规划到的 / 版本适配 / 未知） | 解决方案 |
|---|---|---|
| 1 | **`npm install` 报 `Cannot read properties of null (reading 'edgesOut')`**（npm 10 的 arborist 在复杂 peer 图上崩） | 改用 **pnpm 12**（harness 生态同款）；`pnpm install` 成功（116 包，`@deepseek-ai/*` 0.1.5-rc.1 / `@deepseek-ai/cordis` 4.0.2） |
| 2 | **tsdown 0.22：`external` 已废弃** → 用 `deps.neverBundle`；`noExternal` → `deps.alwaysBundle` | 改 `deps: { neverBundle: [/^@deepseek-ai\//, /^pg$/] }` 等；warning 消失 |
| 3 | **tsdown `entry` 是对象映射不是数组对象**：`entry: { index: 'src/index.ts' }` | 修正 tsdown.config.ts |
| 4 | **client bundle 必须包 `window.__ModuleLoader__.load({ id, factory })`**（官方 DSH client preset，参考 DSH-better-sidebar） | banner/footer 包装；**id = package.json `name`**（`dsh-memory-pg`） |
| 5 | **TS5097 `import path can only end with '.ts'`** → `allowImportingTsExtensions` + `rewriteRelativeImportExtensions` | tsconfig 补两项；产物自动 `.js` |
| 6 | **TS2686 `React refers to a UMD global`**（client .tsx 用 React 但没 import） | `import React from 'react'`（tsdown 会 external react，运行时由 web shell 模块表提供） |
| 7 | **schemastery：`z.any().optional()` 不存在**（`Property 'optional' does not exist`） | 移除 `lastTest` 持久化字段（瞬态 UI 态，不进 schema）；只在 TS 类型里保留 |
| 8 | **`ctx.settings` 类型不存在**（Context 没这属性） | `import '@deepseek-ai/dsh-settings'` 加载其 `declare module '@deepseek-ai/cordis'` 增强（`interface Context { settings }`）；`ctx.inject(['settings'], cb)` 是 cordis 标准 API |
| 9 | **vitest：加载 vitest.config.ts 时 rolldown 崩**（`windowsSafeRealPathSync` / `UNRESOLVED_ENTRY`） | 本环境移除 config 文件，vitest 用默认配置即可（`include tests/**/*.spec.ts` 是默认）；⚠️ vitest fork worker 需 danger-full-access（spawn EPERM） |
| 10 | **测试断言语义错误**：`clampInt(0,1,65535,54320)` 返回 **1**（钳到边界）不是默认值；越界数字→边界、非法类型→默认  | 修正断言：`dbPort:0`→1、`dbPort:70000`→65535；类型非法（`'bad'`）→默认 |
| 11 | **构建双入口歧义**：tsc 产 `lib/index.js` + tsdown 产 `lib/index.mjs` | package.json `main`/`exports` 指向 `lib/index.js`（tsc 产物，符合 `.ts`→`.js` 重写）；`.mjs` 冗余但无害 |
| 12 | **npm pack 后 tar 列出报错**（路径问题） | 无关紧要——安装已经成功；用 `npm pack` 的 tgz 路径直接 `dsh plugin add` |
| 13 | **后台 job 无法升级沙箱**（spawn EPERM 复现于后台 tsx/esbuild） | 启动测试实例一律用前台 `Start-Process` + danger-full-access（保持运行供用户调试） |

### 阶段实现框架

```
dsh-memory-pg/
├── package.json      # dsh.bundle.patch + dsh.client(platform web) + main:lib/index.js + pg/schemastery deps + @deepseek-ai peer
├── cordis.patch.yml  # - insert: - id: memory-pg  name: 'dsh-memory-pg'
├── tsconfig.json / tsdown.config.ts / vitest
├── src/
│   ├── prefs.ts      # 设置类型/默认值/parsePrefs 防御解析（纯逻辑，可单测）
│   ├── config.ts     # schemastery PrefsSchema（表单事实来源）
│   ├── index.ts      # Node half：settings.register + fenced 路由(settings.get/update/connection.test) + 连接测试(pg 懒加载)
│   └── client/index.tsx  # Client half：settings.section list 槽(id=SETTINGS_NS, label='dsh_memory_pg') + DB/embedding 表单 + 连接测试按钮
└── tests/prefs.spec.ts   # parsePrefs 3 用例
```

- **设置接入**：`settings.section` 是 **list 槽**（`{name,id,order,label}` 注册，一个列表项=一个设置页）→ 用户要求的「左侧列表出现」；`settings.plugin.item` 是 keyed 槽（插件卡片，另一条路径，本插件未用）。
- **fenced 路由**：settings RPC 只服务白名单 ns，客户端必须走插件自建 HTTP 路由（`/memory-pg/api/*`）读写在进程内调 seam。
- **隐私化**：本文档不写生产 `<dshhome>` 内具体业务路径；连接串保留 `127.0.0.1:54320 / postgres / czq / dsh_memory_pg`（可运行信息）。

### 调试与隔离方案（M1 段，源码 → 登录 → 操作）

1. **构建 + 测试 + 打包**（工作目录 `<workspace>\dsh-memory-pg`）：
   ```powershell
   npm run build        # tsc + tsdown → lib/
   npx tsc --noEmit     # typecheck
   npx vitest run       # 单测（需 danger-full-access，fork worker）
   npm pack             # → dsh-memory-pg-0.1.0.tgz
   ```
2. **安装到隔离 profile**（工作目录 `<harness-root>`）：
   ```powershell
   $env:DSH_HOME = "<workspace>\.testhome"
   node --import tsx/esm apps/cli/src/bin.ts plugin --profile m0test add "<workspace>\dsh-memory-pg\dsh-memory-pg-0.1.0.tgz"
   node --import tsx/esm apps/cli/src/bin.ts --profile m0test --dump-config | findstr memory-pg   # 确认挂载
   ```
3. **启动隔离实例 → 登录 → 操作**：
   ```powershell
   cd <harness-root>
   $env:DSH_HOME = "<workspace>\.testhome"
   # 前台 Start-Process 保持运行（danger-full-access）
   Start-Process node -ArgumentList "--import","tsx/esm","apps/cli/src/bin.ts","--profile","m0test","--no-open","--port","3099" -WorkingDirectory "<harness-root>" -RedirectStandardOutput "<workspace>\.testhome\web-m0test.log" -RedirectStandardError "<workspace>\.testhome\web-m0test.err.log"
   # 读 ready 行拿 token URL
   Get-Content "<workspace>\.testhome\web-m0test.log"
   # → dsh web: http://127.0.0.1:3099/?token=<43字符>
   ```
4. **浏览器操作**：打开 token URL → 左下角设置 ⚙️ → 左侧列表出现 `dsh_memory_pg` → 填入/确认连接（127.0.0.1:54320 postgres/czq/dsh_memory_pg）→ 「连接测试」看分项 → 「保存」。

### token 使用情况

无法统计上传/下载 token 分项（会话无计量工具），不填。

项目经理人工备注:  从立项, 需求拆解, 设计, task拆分, 到M0验证结束. M1验证结束; 
按照dsh页面下显示的 约88.6M token 
<br> Token 用量 88,556,659 tok 缓存命中 96%
<br> -未缓存输入 3,538,727 tok
<br> -缓存读取 84,839,680 tok
<br> -输出 178,252 tok

### 使用的插件和 skill

- Skill：`dsh-plugin-dev`（bundle 形态 / tool-plugin / build-pitfalls / bundle-patch）、`cordis-plugin-development`（inspect 契约核实 / 动态插件探测）
- 参考实现（本地仓库阅读）：`DSH-better-sidebar`（bundle 形态 + settings.section + tsdown 模式）、`dsh-local-vector-memory`（业务逻辑参考，M2 起用）
- 动态插件：`mopr-1`（M0 遗留，已 stop，定义保留）

---

## M2 — 存储层

> 时间：2026-09-14 16:30–17:09:56（北京时间，时分秒精度）

### 踩坑记录

| # | 问题（前期没规划到的 / 版本适配 / 未知） | 解决方案 |
|---|---|---|
| 1 | **AGE preload 导致 TRUNCATE 失败**：`dsh_memory` 容器 `shared_preload_libraries=age`，新测试库无 `ag_catalog` schema，任何 `TRUNCATE` 报 `schema "ag_catalog" does not exist`（AGE 钩子拦截 TRUNCATE） | 诊断：`SHOW shared_preload_libraries` 确认 `age`；`my_pgvector`(5433) 无 AGE → **单测改用 5433**（README §8.4 方式二）；生产仍用 54320 |
| 2 | **search_path 继承**：54320 库级 search_path 含 `ag_catalog, "$user", public, tiger...` | 连接池启动参数 `options: '-c search_path=public'`（libpq 启动即固定，比 `pool.on('connect')` 异步 handler 更稳——后者与首查询有竞态） |
| 3 | **ILIKE 初筛对中文失效**：content 是三要素空格连接（`用户 偏好 简洁回复`），查询 `用户偏好` 无空格 → `ILIKE '%用户偏好%'` 0 命中 | 改**全表扫 + keywordScore（CJK 二元组感知）**，与参考实现同构；trigram 索引保留作未来大库粗筛（§3.5 小规模全表扫够用论证） |
| 4 | **getFactById 漏软删除过滤**：软删后按 id 仍返回行 | 补 `AND deleted_at IS NULL` |
| 5 | **SCRAM 密码**：`my_pgvector` 密码也是 `czq`（非空）；空串密码触发 `client password must be a string` | 测试 DB 配置用 `czq`；端口/密码做成 `DSH_TEST_PG_*` 环境变量可覆盖 |
| 6 | **vitest 在无 config 时默认 include `tests/**/*.spec.ts`**（本环境加载 vitest.config.ts 会 rolldown 崩，沿用 M1 的"无 config"） | 保持无 config；16 用例 4.5s |
| 7 | **tsdown 警告 `noExternal` 已废弃**（应 `deps.alwaysBundle`） | 记录；不阻塞构建，后续顺手清 |

### 阶段实现框架

```
src/schema.ts   # EXTENSION_SQL + SCHEMA_SQL（四表幂等）
src/store.ts    # MemoryStore：connect(pool+search_path) / migrate / health / truncateAll /
                #   addFact(去重)/getFactById/softDeleteFact/restoreFact/supersedeFact/updateFact/
                #   searchFacts(扫+keywordScore)/searchAndRerank(rerankHits)
src/rerank.ts   # rerankHits(scorer 注入) + heuristicScore 兜底
tests/store.spec.ts   # 9 用例跑真实 PG（5433 my_pgvector，DSH_TEST_PG_* 可覆盖）
tests/rerank.spec.ts  # 7 用例纯逻辑
```

- 隐私化：单测连接串 `127.0.0.1:5433 postgres/czq`（可运行信息保留）；生产 54320 `dsh_memory` 留在文档/配置层。
- 测试隔离：`beforeEach truncateAll()`（四表清空保留结构），用例间无脏数据。

### 调试与隔离方案（M2 段）

1. **单测库**：`my_pgvector`(5433) 建 `dsh_memory_pg_test`（无 AGE，TRUNCATE 可用）。
   ```powershell
   docker exec my_pgvector psql -U postgres -c "CREATE DATABASE dsh_memory_pg_test;"
   ```
2. **跑单测**（工作目录 `<workspace>\dsh-memory-pg`，需 danger-full-access）：
   ```powershell
   npx tsc --noEmit
   npx vitest run            # 16/16；DSH_TEST_PG_PORT/PASSWORD 可覆盖
   npm run build             # 产物 lib/index.js + lib/client.js
   ```
3. **生产库（54320）注意**：业务读写 OK（M0 已验证建表/写入/查询），但 TRUNCATE 测试隔离受限 → 测试隔离一律走 5433。

### 决策记录（M2）

> 后续开发中所有**自动决策**（有备选方案权衡后选定）都在此沉淀：问题 → 思考过程 → 最终决策。

#### D-M2-1 中文检索：不用 ILIKE 初筛，改全表扫 + keywordScore（CJK 二元组）

- **问题**：`searchFacts` 初版用 `content ILIKE '%<q>%'` 做 trigram 初筛，对英文 OK，但对中文**失效**——facts.content 是三要素空格连接（如 `用户 偏好 简洁回复`），查询 `用户偏好`（无空格）时 `ILIKE '%用户偏好%'` 0 命中。
- **思考过程（备选）**：
  - A. **保留 ILIKE 初筛 + 中文空格归一化**：查询前把 content 和 q 都去空格再比。缺点：`gin_trgm_ops` 对去空格后的长中文串效果弱（trigram 需要连续 3 字符，中文语义片段常 <3），且归一化要在 SQL 侧写表达式，可维护性差。
  - B. **全表扫 + keywordScore 内存打分**（参考实现 dsh-local-vector-memory 同构）：按 workspace 拉全部有效 facts，用 CJK 二元组（≤4 字整串、>4 字二元组）打分。缺点：无索引扫描；**但 §3.5 已论证 workspace 级记忆量小（几十~几百条），全表扫 + 内存打分足够**。
  - C. **pg_trgm similarity 操作符（`%` / `similarity()`）**：保留索引但语义是"相似度"，对短查询和中文仍不稳，且阈值难调。
- **最终决策**：选 **B**。理由：与参考实现同构（已被验证可用）；CJK 空格/分词问题彻底规避；workspace 小规模下无索引扫描可接受；trigram 索引**保留不删**（作未来大库粗筛 + `/memory-pg-search` 扩展点）。检索正确性 > 索引微优化。
- **后续提示**：若未来 memory 量级上千（§3.5 向量才真正赢的场景），应升级到「pg_trgm 粗筛 + LLM 重排」或开向量，届时重新评审。

#### D-M2-2 单测 PG 库：用 5433 `my_pgvector`（无 AGE），生产仍用 54320 `dsh_memory`

- **问题**：单测需要 `TRUNCATE` 清表隔离，但 54320 容器 `shared_preload_libraries=age` 使 TRUNCATE 报 `schema ag_catalog does not exist`。
- **思考过程**：A. 在测试库补建 `ag_catalog` schema（hack，AGE 版本行为不确定）；B. 用本机另一无 AGE 的 `my_pgvector`(5433)（README §8.4 方式二已记载）；C. 改测试不用 TRUNCATE（逐条 delete，慢且不彻底）。
- **最终决策**：选 **B**。单测走 5433（干净、TRUNCATE 可用），生产走 54320（AGE 完整）。单测端口/密码做成 `DSH_TEST_PG_*` 环境变量，本地/CI 可切换。

### token 使用情况

无法统计上传/下载 token 分项（会话无计量工具），不填。人工统计见上（M2 段 token 小节）。

### 使用的插件和 skill

- Skill：`dsh-plugin-dev`（build-pitfalls / bundle-patch）、`cordis-plugin-development`（inspect 契约）
- 参考实现：`dsh-local-vector-memory/lib/store.mjs`（CRUD/检索语义）、`lib/extract.mjs`（M3 用）
- PG 容器：`dsh_memory`(54320, AGE) + `my_pgvector`(5433, 无 AGE)

---

## M3 — 提炼与保存

> 时间：2026-09-14 17:50–18:31:44（北京时间，时分秒精度）

### 踩坑记录

| # | 问题 | 解决方案 |
|---|---|---|
| 1 | **一度照搬官方机制**：为补"与官方差距"，把前缀缓存复用/maxTokens 截断/finish 分类/保留近况全搬进 distill，caller 契约从 `(system,user)=>text` 膨胀为 `(messages)=>LlmCallResult` | **用户明确指示回退**：这些是官方自动压缩场景的机制，与"用户手动提炼"目标不匹配。回退到简单 caller（D-M3-2） |
| 2 | **`ctx.get('commands')` 返回 unknown** → handler 参数隐式 any | `import '@deepseek-ai/dsh-commands'` 加载 Context 增强（`interface Context { commands }`） |

### 阶段实现框架（回退后）

```
src/distill.ts   # DISTILL_SYSTEM_PROMPT(三要素JSON + 吸纳官方prompt纪律) / splitContext /
                 #   parseDistilledFacts(三级容错) / distill(context, caller) 简单caller
src/segment.ts   # segmentFacts(过长递归/过短合并/精确去重) + classifyDedup(0.92去重/0.86冲突) + jaccardSimilarity
src/commands.ts  # 五条命令注册（save/compact-save/compact/search/load占位），依赖注入可测
src/store.ts     # + addMessage(原始日志层) + listExistingFacts(冲突检测)
src/index.ts     # distillCaller(ctx.llm包装: system+user) + contextProvider(deriveMessages最近40条) + sessionMeta
docs/compaction-gap.md  # 差距分析 + 采纳策略（只吸收prompt纪律，不照搬机制）
tests/distill.spec.ts / segment.spec.ts  # 纯逻辑用例 + prompt纪律守护
```

- 隐私化：单测 mock caller（不真调模型）；生产 ctx.llm 路径已接线。
- 决策原则：**底座是我们的**（三要素 JSON → facts 表）；只吸纳官方 prompt 纪律（保留精确路径/命令/错误串/标识符/数值；忠实记录纠正/偏好）；不照搬官方机制（前缀缓存复用/自动触发/8节结构/maxTokens/失败分类）。

### 调试与隔离方案（M3 段）

1. 单测全纯逻辑（distill/segment mock caller），无需 DSH、无需真模型。
2. 命令真实冒烟（走 ctx.llm）需在运行实例（3099）里发 `/memory-pg-save` —— L3/L4 待你审批后在测试实例验证。

### 决策记录（M3）

#### D-M3-1 与官方 compaction-basic 的差距分析（问题→思考→决策）

- **问题**：用户要求"看下现在和 dsh-compaction-basic 的差距"。
- **思考**：读官方 `summarizer.ts`（prompt/缓存复用/失败分类）、`config.ts`（阈值/保留/逐模型 policy）、`index.ts`（压力触发/溢出重试）——官方是**自动压缩引擎**（checkpoint 让另一模型恢复工作），我们是**手动提炼入库**。
- **产出**：`docs/compaction-gap.md` 差距总览（8 维度）+ 自身优化点（5 项，本插件核心价值）。

#### D-M3-2 采纳策略：只吸纳官方 prompt 纪律，不照搬机制（问题→思考→决策）

- **问题**：初版把差距分析当成了"要补齐的清单"，把官方 P1 四项（前缀缓存/保留近况/maxTokens/失败分类）全搬进 distill——**被用户纠正：不是照抄官方**。
- **思考**：
  - A. **全量照搬官方机制**（前缀缓存/自动触发/8节结构/maxTokens/失败分类）：这些是官方**自动压缩场景**的工程细节——前缀缓存收益依赖"提炼请求 = 会话延续"；自动触发与官方 compaction 双写冲突；8 节 checkpoint 结构不匹配我们 facts 数据模型。手动触发场景下收益不确定甚至有害。
  - B. **只吸纳官方 prompt 纪律**（保留精确路径/命令/错误串/标识符/数值；忠实记录用户纠正/偏好），机制全不搬。
  - C. 什么都不吸收：丢了官方提炼质量上的验证过的好纪律。
- **最终决策**：选 **B**。回退 distill 到简单 caller（`(system,user)=>text`，context 最近 40 条）；`DISTILL_SYSTEM_PROMPT` 吸收官方 2 条纪律（有单测守护）；`docs/compaction-gap.md` 重写为"差距分析 + 采纳策略"，明确列出 7 项不采用的机制及理由。
- **后续提示**：若 v2 做自动注入，压力计量**复用官方 `ctx.tokenMeter` + `ctx.compaction`**，不重复造轮子（README §2.2①）；提炼频繁失败时才考虑加 maxTokens/finish 分类（届时是通用健壮性，非照搬）。

### token 使用情况

无法统计上传/下载 token 分项（会话无计量工具），不填。

### 使用的插件和 skill

- Skill：`dsh-plugin-dev`（build-pitfalls / tool-plugin）、`cordis-plugin-development`（inspect 契约 / sessions/commands 服务）
- 参考实现：官方 `@deepseek-ai/dsh-compaction-basic`（summarizer/config/index，差距基准）、`dsh-local-vector-memory/lib/extract.mjs`
- PG：`my_pgvector`(5433) 单测；`dsh_memory`(54320) 生产

---

## 后续阶段待补

- M4（注入 / F-13、F-15）：待 M3 审批后启动。
